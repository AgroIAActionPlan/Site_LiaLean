# ============================================
# Variáveis de Ambiente - Produção Docker
# ============================================
#
# Este arquivo contém todas as variáveis necessárias
# para rodar o site LiaLean em produção com Docker.
#
# IMPORTANTE:
# 1. Copie este arquivo para .env
# 2. Preencha TODAS as variáveis marcadas com "PREENCHER"
# 3. NUNCA commite o arquivo .env no Git
#
# ============================================

# ============================================
# PostgreSQL Configuration
# ============================================

# Usuário do banco
POSTGRES_USER=appuser

# Senha do banco (PREENCHER COM SENHA FORTE)
POSTGRES_PASSWORD=PREENCHER_SENHA_FORTE_AQUI

# Nome do banco
POSTGRES_DB=lialean_db

# URL completa de conexão (será montada automaticamente)
# Formato: postgresql://usuario:senha@host:porta/database
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# ============================================
# JWT & Security
# ============================================

# Secret para assinar tokens JWT (PREENCHER)
# Gerar com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=PREENCHER_COM_JWT_SECRET_GERADO

# ============================================
# Application Configuration
# ============================================

# Ambiente
NODE_ENV=production

# Porta interna do container
PORT=3000

# Host (sempre 0.0.0.0 em Docker)
HOST=0.0.0.0

# ============================================
# Application Info
# ============================================

# ID da aplicação
VITE_APP_ID=lialean

# Título da aplicação
VITE_APP_TITLE=LiaLean - IA para Agronegócio

# Logo da aplicação (caminho relativo ou URL)
VITE_APP_LOGO=/logo.png

# ============================================
# Owner (Administrador)
# ============================================

# Nome do proprietário/admin
OWNER_NAME=Admin LiaLean

# OpenID do proprietário (opcional)
OWNER_OPEN_ID=

# ============================================
# OAuth (Opcional - Manus)
# ============================================

# URL do servidor OAuth (deixar vazio se não usar)
OAUTH_SERVER_URL=

# URL do portal OAuth (deixar vazio se não usar)
VITE_OAUTH_PORTAL_URL=

# URL da API Forge (deixar vazio se não usar)
BUILT_IN_FORGE_API_URL=

# Chave da API Forge (deixar vazio se não usar)
BUILT_IN_FORGE_API_KEY=

# ============================================
# Analytics (Opcional)
# ============================================

# Endpoint de analytics (deixar vazio se não usar)
VITE_ANALYTICS_ENDPOINT=

# ID do website no analytics (deixar vazio se não usar)
VITE_ANALYTICS_WEBSITE_ID=

# ============================================
# Domínio e URLs
# ============================================

# Domínio principal (usado para CORS, cookies, etc)
DOMAIN=app.vps.lialean.cloud

# URL completa da aplicação
APP_URL=https://app.vps.lialean.cloud

# ============================================
# INSTRUÇÕES
# ============================================

# 1. GERAR JWT_SECRET:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 2. DEFINIR SENHA FORTE PARA POSTGRES_PASSWORD:
#    Use no mínimo 16 caracteres com letras, números e símbolos
#    Exemplo: openssl rand -base64 32
#
# 3. VERIFICAR DOMÍNIO:
#    Certifique-se que app.vps.lialean.cloud aponta para seu servidor
#
# 4. SALVAR COMO .env:
#    cp .env.production.docker .env
#    nano .env  # Editar e preencher
#
# 5. PROTEGER O ARQUIVO:
#    chmod 600 .env
#
# 6. NUNCA COMMITAR:
#    Adicione .env ao .gitignore
#
# ============================================

